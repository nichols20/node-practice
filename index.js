const _ = require("underscore");

const result = _.contains([1, 2, 3], 4);

console.log(result);

//To state that a npm package is a dev dependency after npm i <package>
// you append --save-dev Ex: npm i jshint --save-dev
